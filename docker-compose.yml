version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      LICHESS_TOKEN: ${LICHESS_TOKEN}
    volumes:
      - ./public:/app/public:ro
    restart: unless-stopped
